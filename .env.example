# Application Environment (production or development)
APP_ENV=production

# Database Credentials
DB_HOST=database
DB_USER=root
DB_PASS=strong_password_here

# MySQL Root Password (only used during DB initialization)
MYSQL_ROOT_PASSWORD=strong_password_here
